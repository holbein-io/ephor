spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ephor
    username: ephor
    password: ephor
  jpa:
    show-sql: true

# Enable dev user for local development without oauth2-proxy
auth:
  dev:
    enabled: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
